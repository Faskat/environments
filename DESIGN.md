# Environments — мини-ТЗ на дизайн

> **Статус (26.09.2026):** дизайн получен, лежит в `design/`: логотип, иконки для трея под светлую и тёмную
> панель задач, 34 иконки и `tokens.csv`. Всё это уже вшито в приложение. Светлая тема из токенов пока не подключена.

## Что это
Приложение для Windows, которое живёт в трее. Пресет («Игра», «Учёба», «Код»…) при включении
**закрывает все лишние программы** и **запускает нужные**. Пример: «Игра» оставляет Discord, Telegram,
Steam и саму игру, остальное закрывает. Включается горячей клавишей, из трея или кнопкой в окне.

Аудитория — геймеры и школьники/студенты, которые переключаются между «играю», «учусь», «кодю».
Настроение: тёмное, спокойное, «игровой софт без кислоты». Ориентиры: Discord, Raycast, Linear, Steam Deck UI.

## Что нужно сделать
1. **Логотип и иконка приложения.** Сейчас стоит заглушка: сетка 2×2 окна, два окна горят, два потушены
   (идея «оставить нужное»). Можно развить её или предложить своё.
   Отдать: SVG + PNG 16, 20, 24, 32, 48, 64, 256 px. Отдельно проверить читаемость на 16 px в трее,
   на светлой и тёмной панели задач.
2. **Главное окно** — 4 вкладки (ниже по экранам).
3. **Окно предпросмотра** пресета.
4. **Окно «Из запущенных»** (выбор программ).
5. **Меню в трее** и **уведомление** с итогом.
6. **UI-кит**: цвета, шрифты, кнопки, поля, переключатели, чипы, карточки, чекбоксы, скроллбар.

## Экраны

### 1. Пресеты (главный экран)
- Слева — список пресетов: цветной квадрат со значком, название, горячая клавиша. Внизу «+ Новый пресет».
- Справа — редактор выбранного пресета:
  - шапка: большой значок (эмодзи) в цвете пресета, название (редактируется на месте), 8 цветов на выбор;
    действия «Копия», «Экспорт», «Импорт», «Удалить» и главная кнопка **«▶ Включить»** в цвете пресета;
  - блок «Что оставить и что запустить»: поиск + кнопка «Из запущенных»; два списка — **Группы**
    (Мессенджеры, Браузеры, Лаунчеры, Игры…) и **Приложения** (Discord, Steam, OBS…).
    В каждой строке: иконка программы, название, подпись (состав группы / «запущено»),
    переключатель **«Оставить»** и переключатель **«▶ Запустить»**;
  - «Как закрывать» — выбор из трёх карточек: Аккуратно / Умно (по умолчанию) / Жёстко (опасный, красный акцент);
  - «Ждать закрытия N сек», поле горячей клавиши (режим записи «Нажми сочетание…»), два чекбокса:
    «Сворачивать вместо закрытия», «Закрывать и фоновые программы».
- Состояния строки: не выбрано · «Оставить» · «Оставить + Запустить» · «в группе» (приложение уже
  оставлено через группу — вместо переключателя серая подпись) · «Запустить» недоступно (нечем запускать).
- Предупреждения: горячая клавиша совпадает с другим пресетом / занята другой программой.

### 2. Приложения
Список слева (иконка + название), справа карточка: значок, название, «Как узнать программу»
(правила построчно, моноширинный текст), «Как запускать» (пути/ссылки построчно),
кнопка «▶ Проверить запуск» со статусом, «В группах». Нужна подача, чтобы правила не пугали
новичка: подсказки, примеры, возможно чипы вместо голого текста.

### 3. Группы
Список групп слева; справа значок, название и чек-лист приложений с иконками.

### 4. Настройки
Чекбоксы: автозапуск с Windows, «горячая клавиша сначала показывает предпросмотр», уведомления.
Большое поле «Никогда не закрывать» (защищённые процессы). Кнопки «Открыть папку с настройками»,
«Сбросить всё к встроенному» (опасная).

### 5. Предпросмотр (≈560×640, поверх всех окон)
Шапка со значком и названием пресета, режим закрытия подписью. Список «Закроется (N)» с галочками,
иконкой, названием и заголовком окна; «Снять все / Выбрать все». «Останется» и «Запустится»
(с пометкой «уже запущено»). Кнопки «Отмена» / «Включить».
Состояния: загрузка («Ищу открытые программы…»), закрывать нечего, идёт закрытие (прогресс текстом),
**итог** (закрыто / добито из трея / не закрылось — ждёт сохранения / запущено / ошибки)
с кнопками «↩ Вернуть как было» и «Готово».

### 6. Трей
Контекстное меню: пресеты со значками и горячими клавишами, «↩ Вернуть как было», «Открыть», «Выход».
Уведомление Windows после пресета: «🎮 Игра — закрыто 5, запущено: Discord, Steam».

## Текущие токены (можно менять)
- Фон `#15161A`, поверхность `#1D1F25`, поверхность-2 `#262932`, рамка `#30333D`.
- Текст `#ECEDEF`, второстепенный `#8D919C`, акцент `#7C8CFF`, опасное `#FF6B6B`.
- Цвета пресетов: `#57D68D` `#FF6B6B` `#FFC857` `#7C8CFF` `#FF9F43` `#C38BFF` `#4FD1E8` `#9AA0A6`.
- Шрифт Segoe UI Variable, 14 px основной, 12.5 подписи, 15 подзаголовки, 22 заголовки. Скругления 8 / 12 / 14.

## Ограничения движка (WPF, Windows)
- **Эмодзи рисуются одноцветными** — значки пресетов и групп лучше заменить на свой набор иконок
  (нужен SVG-сет ~24 шт.: игра, запись, учёба, код, 3D, чилл, метла, мессенджер, браузер, музыка, школа…).
- Иконки программ берутся из самих exe — дизайн должен выдерживать чужие пёстрые иконки.
- Минимальный размер главного окна 900×560, по умолчанию 1120×760; заголовок окна системный (тёмный).
- Анимации — простые (fade, slide, цвет); без blur за окном.
- Пока только тёмная тема. Светлая — по желанию, как второй набор токенов.

## Что отдать
Figma (или аналог): UI-кит + 6 экранов выше со всеми состояниями, иконка и логотип, набор иконок пресетов/групп.
Экспорт: SVG иконок, PNG/ICO логотипа, таблица токенов (цвет, размер, отступ, радиус).
